Feat: Production overhaul — CANCELLED status, cancel/preview/regenerate endpoints, terminology update

Database:
- Add CANCELLED to status CHECK constraint
- Add cancelled_at, updated_at columns
- Add 5 indexes (uuid, ticket_id, status, created_at, scanned_at)
- Create ticket_id_seq with auto-sync to existing max ID

Backend:
- Use PostgreSQL sequence for concurrent-safe ticket ID generation
- Add cancelTicket (VALID -> CANCELLED with cancelled_at)
- Add previewTicket endpoint (inline Content-Disposition)
- Add regeneratePDF endpoint (preserves UUID/ticket ID)
- Server-side validation on createTicket
- Verify endpoint handles CANCELLED status (single atomic UPDATE)
- All responses use professional {success, message, code} format
- Verify responses preserve `action` field for scanner compatibility

Frontend:
- Terminology: Ticket -> Registration throughout
- Tickets page: add Preview, Regenerate, Cancel action buttons
- CreateTicket: updated success UI with Preview + Download buttons
- Dashboard: add Cancelled stat card, update labels
- Sidebar: Event Manager, Registrations, Create Registration
- Scan.jsx: add CANCELLED result overlay
- ticketService: add preview(), regeneratePDF(), cancel() methods
