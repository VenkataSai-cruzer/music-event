Fix: Production stability — idempotency, error handling, response format alignment

Backend:
- Add client_request_id UUID UNIQUE column for idempotency
- createTicket: check idempotency key before insert, use ON CONFLICT DO NOTHING
- pool.connect() moved into try/catch with safe error message
- Global error handler returns {success, message, code} format (matches API)
- Error messages are user-friendly (no raw DB error exposure)

Frontend:
- Idempotency key via useRef + crypto.randomUUID() (no uuid package dependency)
- Double-submission guard: if (submitting) return at top of onSubmit
- PDF failure handling: shows success with Regenerate button when PDF fails
- fallback: res.data.data || res.data for response format compatibility
- Generate new idempotency key on "Create Another"
