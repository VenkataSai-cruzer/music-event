Feat: DB transaction, event settings, audit log, CSV export, hidden UUIDs, dashboard stats

Database:
- Add event_settings table with seed data (event_name, date, time, venue, address)
- Add activity_log table for audit trail (action, description, ticket_id, performed_by, result)
- Fix old migration: check for columns instead of table existence (prevents ticket wipe on restart)

Backend:
- createTicket now uses proper pool.connect() + client.query() transaction pattern
- getAllTickets no longer exposes qr_token (UUID hidden from admin)
- PDF generator accepts eventSettings parameter (dynamic values, fallback defaults)
- Audit logging: REGISTRATION_CREATED, ENTRY_APPROVED, REGISTRATION_CANCELLED, PDF_REGENERATED
- CSV export endpoint: GET /api/tickets/export/csv
- Event settings CRUD: GET/PUT /api/tickets/event-settings
- Dashboard stats now includes pending and todayRegistrations
- Add PATCH to CORS methods

Frontend:
- Dashboard: 5-column grid with Pending and Today's Registrations stat cards
- Tickets page: Export CSV button with FileSpreadsheet icon
- ticketService: add exportCSV(), getEventSettings(), updateEventSettings()
